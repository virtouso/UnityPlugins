using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Heap : ITree
{
    //min heap/ max heap min.max heap.
    //a tree that every node is smaller than its children
    //a tree that every node is bigger than its children
    //...

    //a tree that is filled except last level
}